description: View multiple (a list of) deals.
enabled: true
entry_point: run.py
name: deal_list
parameters:
  api_action:
    description: deal_list
    required: true
    type: string
  api_key:
    description: Your API key
    required: true
    type: string
  api_output:
    default: json
    description: json
    required: false
    type: string
  end_point:
    default: deal_list
    immutable: true
    type: string
  filters:
    description: See below code example for all filters you can pass.
    required: false
    type: string
  full:
    description: Whether or not to return ALL data, or an abbreviated portion (set
      to 1 for ALL data, and 0 for abbreviated).
    required: false
    type: string
  page:
    description: 'Pagination: results are limited to 20 per page, so specify what
      page to view.'
    required: false
    type: string
  sort:
    description: 'Field to sort on (possible values: id, datetime).'
    required: false
    type: string
  sort_direction:
    description: 'Direction of sort (possible values: ASC or DESC).'
    required: false
    type: string
runner_type: run-python
