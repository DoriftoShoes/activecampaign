description: View contact custom fields (no data).
enabled: true
entry_point: run.py
name: list_field_view
parameters:
  api_action:
    description: list_field_view
    required: true
    type: string
  api_key:
    description: Your API key
    required: true
    type: string
  api_output:
    default: json
    description: xml, json, or serialize (default is XML)
    required: false
    type: string
  end_point:
    default: list_field_view
    immutable: true
    type: string
  ids:
    description: ID's of the contact custom field you want to view (comma-separated
      for many; use 'all' to view all)
    required: true
    type: string
runner_type: run-python
